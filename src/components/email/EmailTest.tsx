// This is a placeholder component to replace the removed email functionality
import React from 'react';

// Simple placeholder component that doesn't do anything
const EmailTest = () => {
  return null;
};

export default EmailTest;
